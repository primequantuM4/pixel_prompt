//TODO: Generate DocString once context becomes more involved
class Context {
  int cursorX = -1;
  int cursorY = -1;

  setInitialCursorPosition(int x, int y) {
    cursorX = x;
    cursorY = y;
  }
}
